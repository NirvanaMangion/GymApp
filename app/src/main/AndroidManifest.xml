<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nirvana.gymapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymApp">

        <!-- Main Activity (Launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Email Signup Activity -->
        <activity
            android:name=".EmailSignupActivity"
            android:label="Sign up"
            android:theme="@style/Theme.Material3.DayNight.NoActionBar"
            android:windowSoftInputMode="adjustPan" />

        <!-- Phone Signup Activity -->
        <activity
            android:name=".PhoneSignupActivity"
            android:label="Sign up with Phone"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Material3.DayNight.NoActionBar"
            android:windowSoftInputMode="adjustPan" />

        <!-- Unit Selection Activity -->
        <activity
            android:name=".UnitSelectionActivity"
            android:exported="false" />

        <!-- Home Page Activity -->
        <activity
            android:name=".HomepageActivity"
            android:exported="false" />

    </application>

</manifest>
